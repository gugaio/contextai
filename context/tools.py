def look_up_playback_api(video_id, player_type):
    """Use to find playback api info.
    Search query can be a description or keywords."""
    return "item_132612938"

def execute_ffmpeg(video_id, player_type):
    print("Summary:", video_id, player_type) # lazy summary
    return "success"